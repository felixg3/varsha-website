{
  "name": "varsha-website",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "check": "astro check"
  },
  "devDependencies": {
    "astro": "^7.2.2",
    "wrangler": "^4.123.0"
  }
}
